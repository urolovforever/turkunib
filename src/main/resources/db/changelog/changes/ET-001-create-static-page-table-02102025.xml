<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="ET-001-create-static-page-table-02102025" author="Ruslan Skaldin">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="static_pages"/>
            </not>
        </preConditions>

        <createTable tableName="static_pages">
            <column name="id" type="bigint unsigned" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="slug" type="varchar(512)">
                <constraints nullable="false"/>
            </column>

            <column name="enabled" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

            <column name="type" type="varchar(20)">
                <constraints nullable="false"/>
            </column>

            <column name="author_id" type="bigint unsigned">
                <constraints nullable="true"/>
            </column>

            <column name="updated_at" type="timestamp">
                <constraints nullable="true"/>
            </column>

            <column name="created_at" type="timestamp">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <addUniqueConstraint tableName="static_pages"
                             columnNames="slug"
                             constraintName="uk_static_pages_slug"/>

        <!-- FK на users -->
        <addForeignKeyConstraint baseTableName="static_pages"
                                 baseColumnNames="author_id"
                                 referencedTableName="users"
                                 referencedColumnNames="id"
                                 onDelete="SET NULL"
                                 onUpdate="CASCADE"
                                 constraintName="fk_static_pages_author"/>

    </changeSet>
</databaseChangeLog>