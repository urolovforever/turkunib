<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="ET-001-create-news-categories-table-060925" author="Ruslan Skaldin">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="news_categories"/>
            </not>
        </preConditions>

        <!-- Create a news_categories table -->
        <createTable tableName="news_categories">
            <column name="id" type="bigint unsigned" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="image_id" type="bigint unsigned">
                <constraints nullable="true"/>
            </column>

            <column name="author_id" type="bigint unsigned">
                <constraints nullable="true"/>
            </column>

            <column name="updated_at" type="timestamp">
                <constraints nullable="true"/>
            </column>

            <column name="created_at" type="timestamp">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <createIndex tableName="news_categories" indexName="idx_news_category_image_id">
            <column name="image_id"/>
        </createIndex>

        <!-- FK на images -->
        <addForeignKeyConstraint baseTableName="news_categories"
                                 baseColumnNames="image_id"
                                 referencedTableName="images"
                                 referencedColumnNames="id"
                                 onDelete="SET NULL"
                                 onUpdate="CASCADE"
                                 constraintName="fk_news_categories_image"/>

        <!-- FK на users -->
        <addForeignKeyConstraint baseTableName="news_categories"
                                 baseColumnNames="author_id"
                                 referencedTableName="users"
                                 referencedColumnNames="id"
                                 onDelete="SET NULL"
                                 onUpdate="CASCADE"
                                 constraintName="fk_news_categories_author"/>

    </changeSet>
</databaseChangeLog>